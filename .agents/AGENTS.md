# Workspace Project Rules

- **Exe & Zip Build Preference**: Do NOT automatically run PyInstaller or update `.exe` / `.zip` release packages during normal code iterations, UNLESS the user explicitly asks to package or build the release files.
